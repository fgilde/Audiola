<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <!-- The web host has no desktop UI dependency and can run on every supported OS. -->
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.Components.WebAssembly.Server" Version="10.0.8" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Audiola.Core\Audiola.Core.csproj" />
    <!-- WASM-Client wird von dieser API gehostet (gleicher Origin → keine CORS/Mixed-Content-Probleme). -->
    <ProjectReference Include="..\Audiola.Web\Audiola.Web.csproj" />
  </ItemGroup>

</Project>
