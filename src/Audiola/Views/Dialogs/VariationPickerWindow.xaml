<ui:FluentWindow
    x:Class="Audiola.Views.Dialogs.VariationPickerWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Variationen anwenden"
    Width="480" Height="540"
    MinWidth="380" MinHeight="420"
    WindowStartupLocation="CenterOwner"
    ResizeMode="CanResize"
    ExtendsContentIntoTitleBar="True"
    WindowBackdropType="Mica"
    WindowCornerPreference="Round">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Variationen anwenden">
            <ui:TitleBar.Icon>
                <ui:SymbolIcon Symbol="Wand24" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <StackPanel Grid.Row="1" Margin="18,8,18,0">
            <TextBlock Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       Text="{Binding ScopeLabel, StringFormat='Ziel: {0}'}" />
            <TextBlock Margin="0,12,0,4" FontWeight="SemiBold" Text="Provider" />
            <ComboBox ItemsSource="{Binding Providers}" DisplayMemberPath="Name"
                      SelectedItem="{Binding SelectedProvider, Mode=TwoWay}" />
            <TextBlock Margin="0,14,0,4" FontWeight="SemiBold" Text="Variationen (mehrere möglich)" />
            <ui:TextBox PlaceholderText="Suchen…" Icon="{ui:SymbolIcon Search24}"
                        Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <ScrollViewer Grid.Row="2" Margin="18,8,18,4" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding VariationsView}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Margin="0,5" IsChecked="{Binding IsChecked, Mode=TwoWay}">
                            <StackPanel Margin="4,0,0,0">
                                <TextBlock Text="{Binding Name}" FontWeight="Medium" />
                                <TextBlock Text="{Binding Description}" FontSize="12" TextWrapping="Wrap"
                                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                            </StackPanel>
                        </CheckBox>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="18,8,18,16">
            <ui:Button Content="Abbrechen" Click="Cancel_Click" Margin="0,0,8,0" />
            <ui:Button Content="Anwenden" Appearance="Primary" Click="Apply_Click" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
