<Page
    x:Class="Audiola.Views.Pages.MasteringPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:controls="clr-namespace:Audiola.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    Title="MasteringPage">

    <ScrollViewer Margin="42,24,42,24" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel MaxWidth="1120" HorizontalAlignment="Left">
            <TextBlock Style="{StaticResource DawPageTitle}" Text="Mastering" />
            <TextBlock
                Margin="0,2,0,20"
                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                Text="EQ → Kompressor → Loudness (LUFS) auf dem aktiven Studio-Mix. Vorschau hörbar, „Master (A/B)“ zum Vergleichen." />

            <!-- Gemeinsames Mastering-Panel (auch vom „Spur mastern“-Dialog genutzt) -->
            <controls:MasteringPanel DataContext="{Binding ViewModel}" />

            <!-- Stapelverarbeitung (nur auf der Seite sinnvoll) -->
            <TextBlock Margin="0,24,0,8" FontWeight="SemiBold" Text="Stapelverarbeitung (mehrere Dateien)" />
            <ui:Card Padding="16">
                <StackPanel>
                    <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"
                               Text="Mehrere Dateien mit den aktuell eingestellten Mastering-Werten verarbeiten. Die Ausgabe wird neben der Quelldatei gespeichert." />
                    <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                        <TextBlock VerticalAlignment="Center" Width="150" Text="Ausgabe-Namensmuster:" />
                        <ui:TextBox Width="220" PlaceholderText="{}{name}_mastered"
                                    IsEnabled="{Binding ViewModel.BulkOverwrite, Converter={StaticResource InverseBooleanConverter}}"
                                    Text="{Binding ViewModel.BulkPattern, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    ToolTip="{}{name} wird durch den Dateinamen ersetzt; die Endung bleibt erhalten." />
                        <TextBlock Margin="8,0,0,0" VerticalAlignment="Center" FontSize="12"
                                   Foreground="{DynamicResource TextFillColorTertiaryBrush}" Text="z. B. {}{name}_mastered" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                        <TextBlock VerticalAlignment="Center" Width="150" Text="Ausgabeformat:" />
                        <ComboBox Width="220" VerticalAlignment="Center"
                                  ItemsSource="{Binding ViewModel.BulkFormats}"
                                  SelectedItem="{Binding ViewModel.BulkFormat, Mode=TwoWay}"
                                  ToolTip="Zielformat erzwingen – z. B. MP3 in WAV umwandeln. „Wie Original“ behält die Endung der Quelldatei." />
                        <TextBlock Margin="8,0,0,0" VerticalAlignment="Center" FontSize="12"
                                   Foreground="{DynamicResource TextFillColorTertiaryBrush}" Text="z. B. MP3 → WAV" />
                    </StackPanel>
                    <CheckBox Margin="0,10,0,0" Content="Originale überschreiben (Namensmuster wird ignoriert)"
                              IsChecked="{Binding ViewModel.BulkOverwrite, Mode=TwoWay}" />
                    <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                        <ui:Button Appearance="Primary" Icon="{ui:SymbolIcon FolderArrowUp24}" Content="Dateien wählen &amp; alle mastern"
                                   Command="{Binding ViewModel.BulkMasterCommand}"
                                   IsEnabled="{Binding ViewModel.IsBulkRunning, Converter={StaticResource InverseBooleanConverter}}" />
                        <ui:ProgressRing Width="22" Height="22" Margin="12,0,0,0" VerticalAlignment="Center" IsIndeterminate="True"
                                         Visibility="{Binding ViewModel.IsBulkRunning, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        <TextBlock Margin="12,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                   Text="{Binding ViewModel.BulkStatus}" />
                    </StackPanel>
                </StackPanel>
            </ui:Card>
        </StackPanel>
    </ScrollViewer>
</Page>
